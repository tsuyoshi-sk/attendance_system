# 勤怠管理システム - フロントエンド

NFCTimecard Web UIの全面リデザイン版です。FastAPI バックエンドと統合された、モダンで使いやすい勤怠管理インターフェースを提供します。

## 🎨 デザインコンセプト

- **シンプルで落ち着いたテーマ**: 労務管理アプリにふさわしい、プロフェッショナルなデザイン
- **読みやすいフォント**: Inter / Noto Sans JP を採用
- **一貫性のあるカラースキーム**: ブルー系をベースにした控えめなアクセントカラー
- **レスポンシブデザイン**: デスクトップ・タブレット・モバイル対応

## 📦 技術スタック

- **フレームワーク**: React 18 + TypeScript
- **ビルドツール**: Vite
- **スタイリング**: Tailwind CSS
- **HTTPクライアント**: Axios
- **日付処理**: date-fns
- **アイコン**: lucide-react
- **グラフ**: recharts (予定)

## 🚀 セットアップ

### 1. 依存関係のインストール

```bash
cd frontend
npm install
```

### 2. 環境変数の設定

`.env`ファイルを作成します:

```bash
cp .env.example .env
```

```.env
VITE_API_URL=http://localhost:8080
```

### 3. 開発サーバーの起動

```bash
npm run dev
```

ブラウザで `http://localhost:5173` にアクセスします。

### 4. ビルド

```bash
npm run build
```

ビルド成果物は`dist/`ディレクトリに生成されます。

## 📱 主要機能

### 1. ダッシュボード
- 現在時刻と勤務状態の表示
- ワンクリック打刻ボタン（出勤・外出・戻り・退勤）
- 今日の打刻履歴
- 勤務時間の自動計算

### 2. 打刻履歴
- 日付範囲でのフィルタリング
- 各日の詳細情報（出勤・退勤時刻、勤務時間）
- CSV出力機能（予定）

### 3. レポート
- 月次サマリー（出勤日数、総勤務時間、平均勤務時間、残業時間）
- 日別詳細テーブル
- グラフ表示（予定）

### 4. カレンダー
- 月別カレンダービュー（開発中）

### 5. 設定
- ユーザープロフィール編集
- パスワード変更（予定）
- アカウント情報表示

## 🎯 API統合

FastAPIバックエンドと以下のエンドポイントで連携します:

```typescript
// 認証
POST /api/v1/auth/login

// 打刻
POST /api/v1/punch

// レポート
GET /api/v1/reports/daily/{date}
GET /api/v1/reports/monthly/{year-month}

// ユーザー
GET /api/v1/employees/me
PUT /api/v1/employees/me
```

## 🎨 デザインシステム

### カラーパレット

```css
/* Primary */
--primary: #3B82F6 (Blue 500)

/* Background */
--background: #F5F7FA (Slate 50)

/* Card */
--card: #FFFFFF

/* Text */
--foreground: #1F2937 (Gray 800)
--muted-foreground: #64748B (Slate 500)

/* Status Colors */
--success: #22C55E (Green 500)
--warning: #EAB308 (Yellow 500)
--danger: #EF4444 (Red 500)
```

### コンポーネントクラス

- `.btn` - 基本ボタン
- `.btn-primary` - プライマリボタン
- `.btn-secondary` - セカンダリボタン
- `.btn-success` - 成功アクション
- `.btn-danger` - 危険なアクション
- `.card` - カードコンテナ
- `.badge` - ステータスバッジ
- `.input` - 入力フィールド

## 📂 ディレクトリ構造

```
frontend/
├── src/
│   ├── components/       # 共通コンポーネント
│   │   └── Layout/      # レイアウトコンポーネント
│   │       ├── Sidebar.tsx
│   │       ├── MainLayout.tsx
│   │       └── PageHeader.tsx
│   ├── pages/           # ページコンポーネント
│   │   ├── LoginPage.tsx
│   │   ├── Dashboard.tsx
│   │   ├── AttendancePage.tsx
│   │   ├── ReportsPage.tsx
│   │   ├── CalendarPage.tsx
│   │   └── SettingsPage.tsx
│   ├── lib/             # ユーティリティ
│   │   └── api.ts       # APIクライアント
│   ├── types/           # TypeScript型定義
│   │   └── index.ts
│   ├── App.tsx          # メインアプリケーション
│   ├── main.tsx         # エントリーポイント
│   └── index.css        # グローバルスタイル
├── public/              # 静的ファイル
├── .env.example         # 環境変数のサンプル
├── package.json
├── tsconfig.json
├── vite.config.ts
└── tailwind.config.js
```

## 🔐 認証フロー

1. ユーザーがログインページでID/パスワードを入力
2. `/api/v1/auth/login`にPOSTリクエスト
3. 成功時、`access_token`をlocalStorageに保存
4. 以降のAPIリクエストに`Authorization: Bearer {token}`ヘッダーを自動付与
5. 401エラー時は自動的にログアウト処理

## 🧪 開発ガイドライン

### コンポーネント作成

```typescript
import React from 'react';

interface MyComponentProps {
  title: string;
  onAction: () => void;
}

const MyComponent: React.FC<MyComponentProps> = ({ title, onAction }) => {
  return (
    <div className="card">
      <div className="card-header">
        <h2>{title}</h2>
      </div>
      <div className="card-content">
        <button onClick={onAction} className="btn btn-primary">
          アクション
        </button>
      </div>
    </div>
  );
};

export default MyComponent;
```

### API呼び出し

```typescript
import apiClient from '../lib/api';

// データ取得
const fetchData = async () => {
  try {
    const data = await apiClient.getDailyReport('2024-01-15');
    console.log(data);
  } catch (error) {
    console.error('Error:', error);
  }
};

// 打刻
const punch = async (type: 'in' | 'out') => {
  await apiClient.punch({
    card_idm: 'WEB_' + Date.now(),
    punch_type: type,
  });
};
```

## 📝 今後の予定

- [ ] グラフ表示機能（recharts）
- [ ] カレンダービューの実装
- [ ] CSV出力機能
- [ ] 管理者画面の実装
- [ ] リアルタイム通知
- [ ] オフラインサポート（PWA）
- [ ] モバイルアプリ連携

## 🤝 コントリビューション

1. Forkする
2. Feature branchを作成 (`git checkout -b feature/amazing-feature`)
3. 変更をコミット (`git commit -m 'Add amazing feature'`)
4. Branchにプッシュ (`git push origin feature/amazing-feature`)
5. Pull Requestを作成

## 📄 ライセンス

MIT License

---

**© 2024 Attendance System Project**
